class SnailModel {
  double x;
  double y;

  double get getX => x;
  set setX(double x) => this.x = x;

  get getY => y;
  set setY(y) => this.y = y;
  SnailModel({required this.x, required this.y});
}
